#data

database:
  driverClass: org.postgresql.Driver
#  user: greencar
#  password: greencar
  user: adminzrpgtgh
  password: show
#  url: jdbc:postgresql://localhost:5432/greencar
  url: jdbc:postgresql://@OPENSHIFT_POSTGRESQL_DB_HOST@:@OPENSHIFT_MONGODB_DB_PORT@/greencar

  properties:
    charSet: UTF-8
    hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate.hbm2ddl.auto: create

server:
  applicationConnectors:
  - type: http 
    bindHost: @OPENSHIFT_DIY_IP@
    port: 8080
  adminConnectors:
  - type: http
    bindHost: @OPENSHIFT_DIY_IP@
    port: 8081
    
# Logging settings.
logging:

  # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
  level: INFO

  # Logger-specific levels.
  loggers:

    # Sets the level for 'com.example.app' to DEBUG.
    com.archi.JPA: DEBUG

  appenders:
    - type: console
